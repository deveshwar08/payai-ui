const Schedule = () => {
    return (
        <div id="schedule" className="row">
            <h2>Ready to start?</h2>
            <div>
                <input type="text" placeholder="Enter email address" className="input-bottom" />
                <button className="btn btn-red">Schedule a Demo</button>
            </div>
        </div>
    );
}

export default Schedule;